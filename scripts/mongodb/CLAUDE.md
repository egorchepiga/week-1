# MongoDB Vector Search — Chrome Extensions Database

> **Навигация:** [< Scripts](../CLAUDE.md) | [< Root](../../CLAUDE.md)

**Статус:** ✅ Готов к использованию

---

## Что это?

Локальная база данных с 5,402 Chrome расширениями + семантический поиск для анализа рынка.

**Ключевые возможности:**
- 5,402 расширения с описаниями
- 2,800+ расширений с JTBD (Jobs-To-Be-Done) данными
- Семантический поиск (384-dim embeddings, all-MiniLM-L6-v2)
- Гибридный поиск (text + semantic)
- Полностью локально (Docker + MongoDB)

---

## Структура модуля

```
scripts/mongodb/
├── CLAUDE.md                 # ★ ТЫ ЗДЕСЬ
├── docker-compose.yml        # Docker конфиг для MongoDB
├── init-mongo.js             # Инициализация БД
├── setup-docker.sh           # Запуск одной командой
│
├── import-data.py            # Импорт 5,402 расширений
├── create-embeddings.py      # Генерация векторов (~49 сек)
└── query-extensions.py       # CLI для поиска
```

---

## Быстрый старт

### 1. Запустить MongoDB
```bash
cd scripts/mongodb
./setup-docker.sh
```

### 2. Импортировать данные
```bash
python3 scripts/mongodb/import-data.py
```

### 3. Создать embeddings
```bash
python3 scripts/mongodb/create-embeddings.py
```

### 4. Использовать
```bash
# Статистика
python3 scripts/mongodb/query-extensions.py stats

# Текстовый поиск
python3 scripts/mongodb/query-extensions.py search "tab manager"

# Семантический поиск (по смыслу)
python3 scripts/mongodb/query-extensions.py semantic-search "organize browser tabs"

# Гибридный поиск (лучшие результаты)
python3 scripts/mongodb/query-extensions.py hybrid-search "productivity tools"

# Расширения с JTBD
python3 scripts/mongodb/query-extensions.py with-jtbd --limit 20

# Конкретное расширение
python3 scripts/mongodb/query-extensions.py get <extension_id>
```

---

## Интеграция в пайплайн уроков

### Когда использовать?

| Урок | Этап | Как использовать MongoDB |
|------|------|--------------------------|
| **Lesson 01** | Выбор идеи | `hybrid-search "ниша"` → найти конкурентов |
| **Lesson 01** | Проверка идеи | `with-jtbd` → понять потребности пользователей |
| **Lesson 02** | После keyword research | Сопоставить ключи с реальными конкурентами |
| **Lesson 03** | Перед разработкой | `get <id>` → изучить конкурента детально |

### Пример интеграции с Keyword Research

```python
# 1. Keyword Analyzer даёт метрики
keyword = "tab manager"
volume_us = 15000
kd = 35

# 2. MongoDB даёт реальных конкурентов
# python3 query-extensions.py hybrid-search "tab manager" --limit 20

# 3. Комбинируем для решения:
# - Volume 15K ✅ (высокий спрос)
# - KD 35% ✅ (средняя конкуренция в SEO)
# - 23 расширения в базе (умеренная конкуренция в CWS)
# - JTBD: "When I have too many tabs..." (понятная потребность)
# → РЕКОМЕНДАЦИЯ: Делать MVP
```

---

## Инструкции для AI

### Когда вызывать MongoDB?

```
ТРИГГЕРЫ для использования MongoDB:

1. Пользователь спрашивает о конкурентах
   → hybrid-search "ниша"

2. Нужно проверить идею из Lesson 01
   → hybrid-search "идея" + анализ JTBD

3. После keyword research нужны реальные данные
   → сопоставить ключи с расширениями в базе

4. Нужно понять потребности пользователей
   → with-jtbd + анализ паттернов

5. Изучить конкретного конкурента
   → get <extension_id>
```

### Как анализировать результаты?

```markdown
## Формат ответа AI после запроса к MongoDB

### 📈 Данные
- Запрос: `hybrid-search "tab manager" --limit 50`
- Найдено: 47 расширений
- С JTBD: 23 расширения

### 💡 Инсайты
1. **Конкуренция средняя**: 47 расширений, но только 8 с >100K users
2. **Главные JTBD**:
   - "When I have too many tabs..." (15 расширений)
   - "When I want to save tabs for later..." (8 расширений)
3. **Возможности**:
   - Cloud sync отсутствует у большинства
   - AI-группировка — только 2 расширения

### 🎯 Рекомендации
- MVP 1: Tab Manager + Cloud Sync
- MVP 2: AI Tab Grouping
- MVP 3: Session Management

### 📋 TOP-3 конкурента
| ID | Users | Слабость |
|----|-------|----------|
| xxx | 500K | Нет sync |
| yyy | 200K | Устаревший UI |
| zzz | 100K | Только Chrome |
```

---

## Агент @chrome-extensions-analyst

Для сложного анализа используй встроенного агента:

```bash
@chrome-extensions-analyst анализируй нишу "PDF tools"
@chrome-extensions-analyst изучи расширение <extension_id>
@chrome-extensions-analyst найди незанятые ниши в productivity
```

**Агент автоматически:**
1. Выбирает правильный тип поиска
2. Анализирует JTBD паттерны
3. Выдаёт структурированный отчёт с рекомендациями

---

## Структура данных

### Документ в MongoDB

```json
{
  "extension_id": "gadafnnkijfmbbmeielphlapddbmgbgo",
  "url": "https://chromewebstore.google.com/detail/...",
  "short_description": "Context menu to close tabs...",
  "full_description": "Full feature description...",
  "jtbd": [
    "When I have too many tabs, I want to close by category...",
    "When I want to close multiple tabs at once..."
  ],
  "embedding": [0.123, -0.456, ...],  // 384 dimensions
  "embedding_metadata": {
    "model": "all-MiniLM-L6-v2",
    "dimension": 384
  }
}
```

### Типы поиска

| Тип | Команда | Когда использовать |
|-----|---------|-------------------|
| **Text** | `search "keyword"` | Точный ключ известен |
| **Semantic** | `semantic-search "query"` | Поиск по смыслу |
| **Hybrid** | `hybrid-search "query"` | Лучшие результаты (рекомендуется) |

---

## Метрики системы

| Метрика | Значение |
|---------|----------|
| Всего расширений | 5,402 |
| С JTBD данными | 2,800+ |
| Размер embedding | 384 dim |
| Модель | all-MiniLM-L6-v2 |
| Время генерации | ~49 сек |
| Стоимость | $0 (локально) |

---

## Требования

- Docker Desktop (запущен)
- Python 3.7+
- `pip install pymongo sentence-transformers`

---

## Troubleshooting

### MongoDB не запускается
```bash
# Проверить Docker
docker ps | grep extensions-mongodb

# Пересоздать
cd scripts/mongodb
docker-compose down -v
./setup-docker.sh
```

### Нет embeddings
```bash
python3 scripts/mongodb/create-embeddings.py
```

### pymongo не найден
```bash
pip install pymongo
```

---

## Связанные файлы

| Файл | Описание |
|------|----------|
| `inputs/app-database/description-results.json` | Исходные описания |
| `inputs/app-database/extensions-with-jtbd.json` | JTBD данные |
| `.claude/agents/chrome-extensions-analyst.md` | Конфиг агента |

---

*Последнее обновление: 2025-12-05*
